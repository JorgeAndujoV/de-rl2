.venv/
.claude/
__pycache__/
*.pyc
*.egg-info/
runs/

# Result artifacts: the small records — summary.csv, training_log.csv,
# episode_results.csv, eval_results.csv, comparison.csv, run_metadata.json — ARE
# committed (they are the record of an experiment). The bulky/regenerable ones
# are not.
slurm_logs/
experiments/**/checkpoints/
experiments/**/trajectories/
# Generated analysis figures (scripts/analyze_experiment.py).
tests/
# Baselines are generated per-experiment by each job (run_experiment.sh, via
# run_baseline --skip-existing); regenerable, rsync back if a local copy is wanted.
baselines/
*.bak.*
cec13-c-code/
cec13 python benchmark/
results/
scripts/MACHINERY.md
